/**
 * Feature flags for staged launches.
 *
 * Field Notes (the house journal) is written and wired but held back from the
 * public site until we're ready to launch it. Flip this to `true` to surface
 * it everywhere at once: nav, footer, sitemap, cross-links, and the routes
 * themselves. Nothing else needs to change.
 */
export const FIELD_NOTES_ENABLED = false;
